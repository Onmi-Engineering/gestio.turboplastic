<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <record id="onmi_view_stock_return_picking_form" model="ir.ui.view">
        <field name="name">Negative Discrepancy</field>
        <field name="model">stock.return.picking</field>
        <field name="arch" type="xml">
            <form>
                <field name="discrep_process" invisible="1"/>
                <field name="move_dest_exists" invisible="1"/>
                <group>
                    <field name="sale_ids" readonly="1" widget="many2many_tags"/>
                </group>
                <group>
                    <field name="picking_id" invisible="0" force_save="1" readonly="1"/>
                    <field name="invoice_ids" invisible="1"/>
                    <field name="invoice_id" domain="[('id', 'in', invoice_ids)]"/>

                    <field name="original_location_id" invisible="1"/>
                    <field name="company_id" invisible="1"/>
                    <field name="location_id" readonly="False"/>

                </group>
                <group invisible="not move_dest_exists">
                    <div class="oe_grey">
                        <p>You have to indicate picking and invoice to reverse and quantities of discrepancy
                            on every lines. With this data the system can do the negative discrepancy.
                        </p>
                    </div>
                </group>
                <group>
                    <field name="product_return_moves" nolabel="1">
                        <list editable="top" create="0">
                            <field name="product_id" options="{'no_create': True, 'no_open': True}" force_save="1"/>
                            <field name="quantity"/>
                            <field name="uom_id" groups="uom.group_uom"/>
                            <field name="move_id" invisible="1"/>
                        </list>
                    </field>
                </group>
                <footer>
                    <button name="create_returns" string="Return" type="object" class="btn-primary" data-hotkey="q" invisible="discrep_process"/>
                    <button name="discrepancy_process" string="Create Discrepancy" type="object" class="btn-primary" data-hotkey="q" invisible="not discrep_process"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                </footer>
            </form>
        </field>
    </record>
    <record id="onmi_act_stock_return_picking" model="ir.actions.act_window">
        <field name="name">Negative Discrepancy</field>
        <field name="res_model">stock.return.picking</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="onmi_view_stock_return_picking_form"/>
        <field name="target">new</field>
    </record>
</odoo>